import React from 'react';
import Notifications from '../Notifications';
import './SocialNotifications.less';

const SocialNotifications = () => (
  <div className={'SocialNotifications'}>
    <Notifications />
  </div>
);

export default SocialNotifications;
